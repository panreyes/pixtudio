[**Up to LCD.DLL**](Lcddll "wikilink")

------------------------------------------------------------------------

Changelog of [LCD.DLL](LCD_DLL "wikilink").

v1.11:

`   Yeah, code is a bit messy, but it seems to work anyway.`

v0.03:

`   Recoded so it's easier in use. Not backwards compatible because previous versions`\
`       have been used by only a few people. See `[`main`
`page`](LCD.DLL "wikilink")`.`

v0.01, v0.02:

`   Created main functions and so on.`

<Category:changelog> <Category:lcddll> <Category:dll>
